import { Tooltip } from "@blueprintjs/core";

// HACK: We need to include the themes somewhere, and this is imported early.
import "@blueprintjs/core/lib/css/blueprint.css";
import "./theme.scss";

export default Tooltip;
